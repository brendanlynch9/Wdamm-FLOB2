# Exact core dependencies used on macOS (Apple Silicon M1/M2) with PyTorch MPS.
# These versions are critical for reproducing the numerical results of the Redundancy Cliff paper.

# PyTorch (Core ML Engine)
torch==2.3.1
torchaudio==2.3.1
torchvision==0.18.1

# Hugging Face Libraries (Model/Data Handling)
transformers==4.57.3
accelerate==1.12.0
datasets==4.4.1

# Numerical Libraries
numpy==1.26.4
scipy==1.16.3
# Note: The kappa_compute_clean.py script relies on numpy and scipy for linear algebra.

# Other relevant libraries
einops==0.8.0
tqdm==4.66.2
# Note: In the original environment, tqdm was installed via a local path, but its pure-python
# nature means version 4.66.2 (the closest stable major release) should suffice for functionality.